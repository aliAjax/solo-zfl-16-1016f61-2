# 活字排版工坊

一个纯前端本地应用，用来维护字模库、在网格版面中落字、检查字模数量是否超用、保存草稿并导出PNG预览图。

支持撤销与重做：覆盖落字、清空版面、字模增删、纸张/方向/网格调整以及草稿载入（含作品名）。顶部按钮显示可撤销/可重做数量，快捷键 `Ctrl+Z`（macOS 为 `Cmd+Z`）撤销，`Ctrl+Shift+Z` 或 `Ctrl+Y` 重做。历史记录保存在浏览器本地，刷新页面后依然可用。

直接打开 `index.html` 即可使用，也可以在本目录启动静态服务访问。
